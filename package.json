{
    "private": true,
    "name": "@helick/local-server",
    "release": {
        "verifyConditions": [
            "@semantic-release/github"
        ],
        "getLastRelease": "@semantic-release/git",
        "publish": "@semantic-release/github"
    },
    "config": {
        "commitizen": {
            "path": "cz-conventional-changelog"
        }
    }
}
